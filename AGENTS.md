# Agent Entry Point

Use `AGENT.md` for the repository instructions.

This file exists because some tools look for `AGENTS.md` by convention. Keep the
actual rules in `AGENT.md` so there is only one source of truth.
